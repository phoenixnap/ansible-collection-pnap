requires_ansible: '>=2.15.0'
plugin_routing:
  modules:
    ip_address_info:
      redirect: phoenixnap.bmc.ip_block_info
    ip_address:
      redirect: phoenixnap.bmc.ip_block
    network_info:
      redirect: phoenixnap.bmc.private_network_info
    network:
      redirect: phoenixnap.bmc.private_network
